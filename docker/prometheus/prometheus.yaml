global:
  scrape_interval:     15s
  evaluation_interval: 15s

# Metrics arrive via Alloy's remote_write — no pull targets needed here.
# Prometheus scrapes itself for uptime / meta-metrics only.
scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]
